import React from "react";
import styled from "styled-components";

/*
// INTEGRATION SOURCE COMPONENT
export const IntegrationSource = (inactive) => {
    return (
        <IntegrationSource inactive={inactive}>
            <IntegrationSourceIcon>{icon}</IntegrationSourceIcon>
        </IntegrationSource>
    );
};

// INTEGRATION ITEM COMPONENT
export const IntegrationItem = (type, name, active) => {
    const icons = {
        github: <RiGithubFill />,
    };

    return (
        <IntegrationItemContainer>
            <IntegrationItemIcon type={type}>{icons[type]}</IntegrationItemIcon>
            <IntegrationItemName>{name}</IntegrationItemName>
            {active != null && active != undefined && (
                <IntegrationItemCheck active={active}>
                    <RiCheckboxCircleFill />
                </IntegrationItemCheck>
            )}
        </IntegrationItemContainer>
    );
};

const IntegrationItemContainer = styled.div`
    display: flex;

    min-height: 3.5rem;

    max-height: 3.5rem;

    align-items: center;

    background-color: ${(props) => props.theme.PRIMARY_ACCENT_COLOR_SHADE_1};

    border-radius: 0.5rem;

    padding: 0rem 1rem;

    margin-bottom: 1rem;
`;

const IntegrationItemIcon = styled.div`
    width: 2.5rem;

    display: flex;

    font-size: 1.8rem;

    opacity: 0.85;
`;

const IntegrationItemName = styled.div`
    font-weight: 500;

    opacity: 0.8;

    font-size: 1.2rem;
`;

const IntegrationItemCheck = styled.div`
    width: 2rem;

    margin-left: auto;

    opacity: ${(props) => (props.active ? 1 : 0.2)};

    color: ${(props) => (props.color ? "#19e5be" : "")};
`;
*/
